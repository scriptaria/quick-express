import { NextFunction, Request, Response } from "express";

export const %CAMEL_CASE_NAME% = async (request: Request, response: Response, next: NextFunction) => {

  // TODO: implement your middleware logic

  next();
};
